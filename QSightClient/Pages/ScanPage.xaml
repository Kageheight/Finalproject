<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="QSightClient.Pages.ScanPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QSightClient.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	<Grid Padding="20">
		<StackPanel Spacing="12">

			<TextBlock
                Text="파일 스캔"
                FontSize="24"
                FontWeight="Bold"/>

			<Border Padding="12" BorderThickness="1" CornerRadius="6">
				<StackPanel Spacing="8">

					<TextBlock Text="분석할 파일 선택"/>
					<TextBlock x:Name="SelectedFileText" Text="선택된 파일 없음" Opacity="0.6"/>

					<Button
                        Content="파일 선택"
                        x:Name="SelectFileButton"
                        Click="SelectFile_Click"
                        Width="120"/>

                    <Button
                        Content="스캔 시작"
                        x:Name="StartScanButton"
                        Click="StartScan_Click"
                        Width="120"
                        IsEnabled="False"/>

                    <Button Content="화이트리스트 등록"
                        Click="OnWhitelistClick"
                        Margin="10"/>

                    <TextBlock Text="─────────────────" Opacity="0.3"/>
					<TextBlock Text="스캔 결과" FontWeight="Bold" FontSize="16"/>

					<TextBlock Text="상태"/>
					<TextBlock x:Name="ScanStatusText" Text="-"/>

					<TextBlock Text="위험도"/>
					<TextBlock x:Name="ScanSeverityText" Text="-"/>

					<TextBlock Text="정적 분석 결과"/>
					<TextBlock x:Name="StaticResultText" Text="-"/>

					<TextBlock Text="Scan ID"/>
					<TextBlock x:Name="ScanIdText" Text="-" Opacity="0.5" FontSize="11"/>

				</StackPanel>
			</Border>

		</StackPanel>

    </Grid>
</Page>
